@model mobileHairdresser.Database.tblLogin
@{
    ViewBag.Title = "ChangePassword";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="ContentContianer">
    <h1>Change Password</h1>
    <div class="ContentWrapper">
        <div class="accessWarning">
            <h1>WARNING</h1>
            <p>This will change your login password!</p>
            <p>Your Password must be long then 63 characters.</p>
            <p>@ViewBag.errorMsg</p>
        </div>
        @using (Html.BeginForm("changePassword", "login", FormMethod.Post))
        {
            @Html.ValidationSummary()
                @Html.HiddenFor(model => model.loginID)
                @Html.PasswordFor(model => model.Password, new { @value = "", @placeholder = "Enter old password." })
                @Html.Password("newPassword", null, new { @value = "", @placeholder = "Enter new password." })
                @Html.Password("confirmPassword", null, new { @value = "", @placeholder = "Confirm new password." })
                <button type="submit">Change Password</button>
        }
    </div>
</div>




